import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "nz.co.quizpoint.mobile",
  appName: "quizpoint",
  webDir: "public",
  bundledWebRuntime: false,
};

export default config;
